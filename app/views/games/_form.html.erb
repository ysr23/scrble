<%= form_for(@game) do |f| %>
  <% if @game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% @game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.collection_select :player1, Member.all, :id, :name%>
  </div>
  <div class="field">
    <%= f.collection_select :player2, Member.all, :id, :name%>
  </div>
  <div class="field">
    <%= f.label :p1_score %><br />
    <%= f.number_field :p1_score %>
  </div>
  <div class="field">
    <%= f.label :p2_score %><br />
    <%= f.number_field :p2_score %>
  </div>
  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :date_played %><br />
    <%= f.datetime_select :date_played %>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
